// ModalComentarios.js
import React from 'react';
import { Modal, Button } from 'react-bootstrap';

const ModalComentarios = ({ mostrar, handleClose }) => {
    return (
       <h1></h1>
    );
};

export default ModalComentarios;
